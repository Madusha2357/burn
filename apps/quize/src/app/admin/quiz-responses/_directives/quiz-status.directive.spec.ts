import { QuizStatusDirective } from './quiz-status.directive';

describe('QuizStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new QuizStatusDirective();
    expect(directive).toBeTruthy();
  });
});
